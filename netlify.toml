[build]
  command = "npm run build"
  publish = "dist"

# React Router uses client-side routing, so every path must fall back to
# index.html or refreshing /app/wallet etc. directly would 404 on Netlify.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
